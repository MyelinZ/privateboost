//! Intentionally empty. This crate exists only for its integration test
//! (`tests/heart_disease.rs`): the headless 4-process end-to-end run with
//! the heart_disease AUC gate. Run it explicitly with
//! `cargo test -p pbr-e2e --release -- --ignored`.
